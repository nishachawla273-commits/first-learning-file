
undoing changes commands<br>
1 case 1 (stage changes mean those changes which r added but didn't commited)<br>
.git reset (file name)// use for any spacific file change undo<br>
.git reset // for all change<br>
2  case 2 (commited changes agter commit how to undo it for one commit only)<br>
.git reset HEAD~1<br>
3 case 3 (for many commited changes)
git reset (commit hsst / no of commit) whic we can see by git log command<br>
. git log// for history of commits




